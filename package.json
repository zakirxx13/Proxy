{
  "name": "m3u8-proxy-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker HLS/M3U8 proxy for authorized streams",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.10"
  }
}
